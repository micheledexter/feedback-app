import React from 'react';
import '../../App/App.css';

// Simple header for the admin page
const AdminHeader = () => (
  <header className="App-header">
    <h1 className="App-title">Feedback Results!</h1>
  </header>
);

export default AdminHeader;