import React from 'react';
import '../../App/App.css';

// Simple header for the user
const UserHeader = () => (
  <header className="App-header">
    <h1 className="App-title">Feedback!</h1>
  </header>
);

export default UserHeader;